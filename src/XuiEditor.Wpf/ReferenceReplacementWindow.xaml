<Window x:Class="XuiEditor.Wpf.ReferenceReplacementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Ui.Xaml.ReferenceReplacementWindow.001}"
        Width="820"
        Height="520"
        MinWidth="680"
        MinHeight="420"
        Background="{StaticResource WindowBrush}"
        Foreground="{StaticResource TextBrush}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="InstructionText"
                   Grid.Row="0"
                   Text="{DynamicResource Ui.Xaml.ReferenceReplacementWindow.002}"
                   Foreground="{StaticResource MutedTextBrush}"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10" />
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="95" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="95" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{DynamicResource Ui.Xaml.ReferenceReplacementWindow.003}" VerticalAlignment="Center" />
            <TextBox x:Name="CurrentValueText"
                     Grid.Column="1"
                     IsReadOnly="True" />
            <TextBlock Grid.Column="2"
                       Text="{DynamicResource Ui.Xaml.ReferenceReplacementWindow.004}"
                       Margin="10,0,0,0"
                       VerticalAlignment="Center" />
            <TextBox x:Name="ReplacementValueText"
                     Grid.Column="3"
                     TextChanged="ReplacementValue_TextChanged" />
            <Button Grid.Column="4"
                    Content="{DynamicResource Ui.Xaml.ReferenceReplacementWindow.005}"
                    Margin="8,0,0,0"
                    Click="Preview_Click" />
        </Grid>
        <DataGrid x:Name="PreflightGrid"
                  Grid.Row="2"
                  Margin="0,10,0,0"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{DynamicResource Ui.Xaml.ReferenceReplacementWindow.006}"
                                    Binding="{Binding FilePath}"
                                    Width="*" />
                <DataGridTextColumn Header="{DynamicResource Ui.Xaml.ReferenceReplacementWindow.007}"
                                    Binding="{Binding NodeDisplay}"
                                    Width="130" />
                <DataGridTextColumn Header="{DynamicResource Ui.Xaml.ReferenceReplacementWindow.008}"
                                    Binding="{Binding PropertyName}"
                                    Width="135" />
                <DataGridTextColumn Header="{DynamicResource Ui.Xaml.ReferenceReplacementWindow.009}"
                                    Binding="{Binding CurrentValue}"
                                    Width="140" />
                <DataGridTextColumn Header="{DynamicResource Ui.Xaml.ReferenceReplacementWindow.010}"
                                    Binding="{Binding ReplacementValue}"
                                    Width="140" />
            </DataGrid.Columns>
        </DataGrid>
        <TextBlock x:Name="StatusText"
                   Grid.Row="3"
                   Margin="0,8,0,0"
                   Foreground="{StaticResource MutedTextBrush}"
                   TextWrapping="Wrap" />
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Content="{DynamicResource Ui.Xaml.ReferenceReplacementWindow.011}" IsCancel="True" MinWidth="85" />
            <Button x:Name="ApplyButton"
                    Content="{DynamicResource Ui.Xaml.ReferenceReplacementWindow.012}"
                    IsEnabled="False"
                    MinWidth="130"
                    Click="Apply_Click" />
        </StackPanel>
    </Grid>
</Window>
